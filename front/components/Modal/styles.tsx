import styled from '@emotion/styled';

export const CreateModal = styled.div`
  position: absolute;
  top: 0;
  bottom: 0;
  left: 0;
  right: 0;
  background-color: rgba(0, 0, 0, 0.6);

  > div {
    position: absolute;
    z-index: 1000;
  }
`;
